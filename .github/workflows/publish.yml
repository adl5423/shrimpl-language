name: Publish to crates.io

on:
  push:
    branches:
      - main

jobs:
  publish:
    name: CI + Publish to crates.io
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Cargo registry and build
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build
        run: cargo build --verbose

      - name: Run tests
        run: cargo test --all --verbose

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      # Dry-run publish but allow a “dirty” working tree (e.g. Cargo.lock differences)
      - name: Dry run publish
        run: cargo publish --dry-run --locked --allow-dirty

      - name: Publish to crates.io
        if: github.ref == 'refs/heads/main'
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        # Same flags here so a dirty Cargo.lock never blocks CI publishing
        run: cargo publish --token "$CARGO_REGISTRY_TOKEN" --locked --allow-dirty
